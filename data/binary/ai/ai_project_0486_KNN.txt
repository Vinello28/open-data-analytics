Sviluppo di un sistema intelligente per super-resolution tramite K-Nearest Neighbors (KNN) e algoritmi di clustering applicato al settore finanza. La piattaforma processa video ad alta definizione 4K/8K con latenza inferiore a 100ms. L'architettura prevede ONNX runtime per deployment cross-platform con ridondanza geografica.